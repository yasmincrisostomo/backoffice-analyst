<!DOCTYPE html>
<html>
  <head>
    <title>Backoffice Analyst Test</title>
    <link rel="stylesheet" type="text/css" href="style.css">
    <link rel="preconnect" href="https://fonts.googleapis.com">
    <link rel="preconnect" href="https://fonts.gstatic.com" crossorigin>
    <link href="https://fonts.googleapis.com/css2?family=Montserrat:wght@300&display=swap" rel="stylesheet">
  </head>
  <body>
    <header>
      <h1>Backoffice Analyst Test</h1>
    </header>

    <main>
      <h2>Average Approval Time</h2>
      <p><%= "%.2f" % @avg_time %> hours</p>

      <h2>Maximum Approval Time</h2>
      <p><%= "%.2f" % @max_time %> hours</p>

      <h2>Minimum Approval Time</h2>
      <p><%= "%.2f" % @min_time %> hours</p>

      <table class="table">
        <thead>
          <tr>
            <th>CNPJ</th>
            <th>Date of Purchase</th>
            <th>Approval Time</th>
          </tr>
        </thead>
        <tbody>
          <% @results.each do |row| %>
            <tr>
              <td><%= row["cnpj"] %></td>
              <td><%= row["date_of_purchase"] %></td>
              <td><%= "#{row["approval_time_hours"].to_i} hours #{row["approval_time_minutes"].to_i} minutes" %></td>
            </tr>
          <% end %>
        </tbody>
      </table>
    </main>

    <footer>
      <p>&copy; <%= Time.now.year %> Backoffice Analyst</p>
    </footer>
  </body>
</html>
